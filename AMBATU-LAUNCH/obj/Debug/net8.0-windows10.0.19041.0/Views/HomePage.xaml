<Page
    x:Class="AMBATU_LAUNCH.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AMBATU_LAUNCH.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Padding="12">
            <TextBlock Text="Icon Size:" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <Slider x:ConnectionId='4' x:Name="IconSizeSlider" Minimum="50" Maximum="200" Value="100" Width="200" />
        </StackPanel>

        <GridView x:ConnectionId='2' Grid.Row="1" x:Name="AppGrid" Padding="12">
            <GridView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Width="{Binding ElementName=IconSizeSlider, Path=Value}" 
                                Height="{Binding ElementName=IconSizeSlider, Path=Value}">
                        <Image Source="{Binding IconPath}" HorizontalAlignment="Center" VerticalAlignment="Center" Stretch="Uniform" />
                        <TextBlock Text="{Binding Name}" HorizontalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>

